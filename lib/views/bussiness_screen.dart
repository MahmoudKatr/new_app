import 'package:flutter/material.dart';

class BussinessScreen extends StatelessWidget {
  const BussinessScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
        body: Center(
      child: Text('Bussiness Screen'),
    ));
  }
}
